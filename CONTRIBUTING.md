# Contributing

```sh
npm ci
npm run check
```

Keep parsing deterministic, preserve stable issue codes unless a breaking
release intentionally changes them, and add focused tests for behavior changes.
Update public documentation when users need to understand a change.
