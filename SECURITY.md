# Security

Report vulnerabilities privately through
[GitHub Security Advisories](https://github.com/Ismail-elkorchi/argv-flags/security/advisories/new).

Treat command-line arguments as untrusted. Check `result.success` before using
parsed values, and branch on `issues[].code` rather than human-readable
messages.
